pkgname=tanx-deps-colors
pkgver=1.0.0
pkgrel=1
pkgdesc="Dependencies for tanx colors config"
arch=('any')
depends=('matugen' 'imagemagick' 'zenity')

package() {
  # Meta-package: no files installed
  return 0
}
